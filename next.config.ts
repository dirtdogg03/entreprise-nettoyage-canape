import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  reactCompiler: true,
  images: {
    unoptimized: true,
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
        pathname: '/**',
      },
    ],
  },
  // Note: Security headers should be configured on hosting platform (Vercel/Netlify)
  // since they are not supported with output: 'export'
};

export default nextConfig;
